# web 빌드 입력값(시크릿 아님). 실제 값은 .env.production에 두고 빌드한다.
# web↔api 서브도메인 분리 — fetch/OAuth 내비게이션이 가리킬 api 절대 오리진.
# 미설정(빈 값) 시 dev 프록시(단일 오리진, 상대경로)로 동작한다.
VITE_API_BASE_URL=https://workout-planner-api.m1nsuppp.com
